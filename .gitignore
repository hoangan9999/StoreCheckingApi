# .NET
bin/
obj/
*.user

# Secrets and data — NEVER commit
.env

# Keep the pgdata folder itself but never its contents: Synology's Container Manager
# refuses to auto-create bind mount folders, so an empty folder has to ship with the
# deploy package.
pgdata/*
!pgdata/.gitkeep

# IDE
.vs/
.idea/
